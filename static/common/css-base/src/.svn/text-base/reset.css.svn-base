@charset "utf-8";

/**
 * Reset & Compatibility
 *
 * @compatible [IE8+ / Chrome / Firefox / Opera / Safari]
 */

/**
 * 1. Change default `box-sizing` to `border-box` + inherit.
 * 2. Hidden a element by `<div hidden></div>` in all browsers.
 * 3. Hide 3d element's backface.
 * 4. IE Fix: Bicubic Scaling for Images.
 */

*{
    margin: 0;
    padding: 0;
}

*,
*:before,
*:after{
    box-sizing: inherit; /* 1 */
    -moz-box-sizing: inherit;
}

ul,
li,
ol{
    list-style: none;
}

fieldset,
img,
iframe{
    border: 0;
}

a::-moz-focus-inner,
button::-moz-focus-inner,
input::-moz-focus-inner{
    border: 0;
    margin: 0;
    padding: 0;
}

article,
aside,
details,
figcaption,
figure,
footer,
header,
hgroup,
nav,
section,
summary{
    display: block;
}

audio,
canvas,
video{
    display: inline-block;
}

audio:not([controls]){
    display: none;
    height: 0;
}

address,
caption,
cite,
code,
dfn,
em,
i,
th,
var{
    font-style: normal;
    font-weight: normal;
}

[hidden]{
    display: none; /* 2 */
}

caption,
th{
    text-align: left;
}

h1,
h2,
h3,
h4,
h5,
h6{
    font-size: 100%;
    font-weight: normal;
}

html{
    font-size: 100%;
    box-sizing: border-box; /* 1 */
    -moz-box-sizing: border-box;
}

body {
   vertical-align: baseline;
   background-color: transparent;
   -webkit-backface-visibility:hidden; /* 3 */
}

a:focus,
a:hover,
a:active,
input:focus,
textarea:focus{
    outline: 0;
}

img{
    vertical-align: middle;
    -ms-interpolation-mode: bicubic; /* 4 */
}

table{
    font-size: inherit;
    font: 100%;
    border-collapse: collapse;
    border-spacing: 0;
}

td{
    vertical-align: top;
}

th{
    text-align: inherit;
}

iframe{
    display: block;
}

textarea{
    resize: none;
    overflow: auto;
    vertical-align: top;
}

button,
input,
select,
textarea{
    font-size: 100%;
    vertical-align: baseline;
}

button,
input{
    line-height: normal;
}

button{
    cursor: pointer;
}

button[disabled],
input[disabled]{
    cursor: default
}

abbr,
acronym{
    border: 0;
    font-variant: normal;
}

strong,
.strong{
    font-weight: bold;
}

[tabindex="-1"]:focus{
    outline: none !important;
}
